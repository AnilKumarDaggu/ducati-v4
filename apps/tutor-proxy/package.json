{
  "name": "@dtea/tutor-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless AI tutor proxy — the slice's entire backend (RDM-002 §2). Sprint 1: SSE hello-world. Sprint 4: Claude tool-use round trip.",
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@fastify/cors": "^10.0.0",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "vitest": "^3.0.0"
  }
}
