{
  "name": "@dtea/sim-core",
  "version": "0.1.0",
  "private": true,
  "description": "Station/step state machines + scoring engine — pure TS, headless-tested (ARCH-002 §7). Implementation lands in Sprint 3 (RDM-003).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@dtea/data": "workspace:*",
    "@dtea/events": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
