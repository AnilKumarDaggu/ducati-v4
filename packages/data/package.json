{
  "name": "@dtea/data",
  "version": "0.1.0",
  "private": true,
  "description": "Shared zod schemas and types — Component, SpecRecord, StepDef, LessonUnit, ExplodeDef, domain events (ARCH-002 §4)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
